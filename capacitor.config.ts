import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'app.hapticx.ktc',
  appName: 'ktc-reborn',
  webDir: '.output/public',
  zoomEnabled: false,
}

export default config
